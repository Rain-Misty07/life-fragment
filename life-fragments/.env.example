# Local
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=life_fragments
PORT=3456

# Railway: add MySQL plugin后变量会自动注入 MYSQLHOST / MYSQLPORT / MYSQLUSER / MYSQLPASSWORD / MYSQLDATABASE
# 也可使用 MYSQL_URL（无需再填上面 DB_*）

# 云端一次性初始化数据库（部署后访问 POST /api/admin/init-db，用完请删除此变量）
# INIT_SECRET=请改成随机长字符串
